
########################################################################################
AUTOGENERATED HYPR CONFIG.
PLEASE USE THE CONFIG PROVIDED IN THE GIT REPO /examples/hypr.conf AND EDIT IT,
OR EDIT THIS ONE ACCORDING TO THE WIKI INSTRUCTIONS.
########################################################################################

#
# Please note not all available settings / options are set here.
# For a full list, see the wiki
#

# autogenerated = 1 # remove this line to remove the warning

# See https://wiki.hyprland.org/Configuring/Monitors/
monitor=eDP-1,2160x1440,0x0,1
monitor=HDMI-A-1,1920x1080@60,2160x0,1
# monitor=HDMI-A-1,1920x1080@75,2160x0,1

#xdg
exec-once=dbus-update-activation-environment --systemd WAYLAND_DISPLAY XDG_CURRENT_DESKTOP
#systemctl
exec-once=/usr/lib/kdeconnectd
exec-once=/usr/lib/polkit-gnome/polkit-gnome-authentication-agent-1
exec-once=~/.config/scripts/other_scripts/portal.sh &
#lock-script
# exec-once=~/.config/scripts/other_scripts/lock.sh

#wallpapers
exec=hyprpaper
#tray
exec-once=waybar &
exec-once=nm-applet --indicator
exec-once=blueman-applet
exec-once=udiskie -ant &
exec-once=kdeconnect-indicator
#notifications
exec-once=swaync
#wl-paste
exec-once=wl-paste -t text --watch clipman store --no-persist
#avizo
exec=avizo-service
#swayosd
# exec=swayosd-server

# See https://wiki.hyprland.org/Configuring/Keywords/ for more

# Execute your favorite apps at launch
# exec-once = waybar & hyprpaper & firefox

# Source a file (multi-file configs)
# source = ~/.config/hypr/myColors.conf

# For all categories, see https://wiki.hyprland.org/Configuring/Variables/
input {
    kb_layout = us,ru
    kb_variant =
    kb_model =
    kb_options = grp:caps_toggle
    kb_rules =

    follow_mouse = 1

    touchpad {
      scroll_factor = 0.3
    }

    sensitivity = 0 # -1.0 - 1.0, 0 means no modification.
}

general {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    gaps_in = 5
    gaps_out = 0
    border_size = 3

    # col.active_border = rgb(44475a) rgb(bd93f9) 90deg
    # col.inactive_border = rgba(44475aaa)
    # col.group_border = rgba(282a36dd)
    # col.group_border_active = rgb(bd93f9) rgb(44475a) 90deg
    # non-gradient alternative
    col.active_border = rgb(bd93f9)
    col.inactive_border = rgba(44475aaa)
    # col.group_border = rgba(282a36dd)
    # col.group_border_active = rgb(bd93f9)

    layout = dwindle
}

decoration {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more

    rounding = 7

    col.shadow = rgba(1E202966)
    # suggested shadow setting
    drop_shadow = yes
    shadow_range = 60
    shadow_offset = 1 2
    shadow_render_power = 3
    shadow_scale = 0.97
}

animations {
    enabled = yes

    # Some default animations, see https://wiki.hyprland.org/Configuring/Animations/ for more

    bezier = myBezier, 0.05, 0.9, 0.1, 1.05

    animation = windows, 1, 7, myBezier
    animation = windowsOut, 1, 7, default, popin 80%
    animation = border, 1, 10, default
    animation = borderangle, 1, 8, default
    animation = fade, 1, 7, default
    animation = workspaces, 1, 6, default
}

# dwindle {
    # See https://wiki.hyprland.org/Configuring/Dwindle-Layout/ for more
    # pseudotile = yes # master switch for pseudotiling. Enabling is bound to mainMod + P in the keybinds section below
    # preserve_split = yes # you probably want this
# }

master {
    # See https://wiki.hyprland.org/Configuring/Master-Layout/ for more
    new_is_master = true
}

gestures {
    # See https://wiki.hyprland.org/Configuring/Variables/ for more
    workspace_swipe = on
    workspace_swipe_invert = yes
    workspace_swipe_cancel_ratio = 0.1
}

misc {
	disable_hyprland_logo = true
	vfr = on
  disable_autoreload = false
}

# Example per-device config
# See https://wiki.hyprland.org/Configuring/Keywords/#executing for more
device:epic mouse V1 {
    sensitivity = -0.5
}

device:gxtp7863:00-27c6:01e0-touchpad {
    sensitivity = 0.0
    natural_scroll = yes
}

# Example windowrule v1
# windowrule = float, ^(kitty)$
# Example windowrule v2
# windowrulev2 = float,class:^(kitty)$,title:^(kitty)$
# See https://wiki.hyprland.org/Configuring/Window-Rules/ for more
# windowrule = float, ^(org.gnome.Calculator)$
# windowrule = float, ^(nm-openconnect-auth-dialog)$



# See https://wiki.hyprland.org/Configuring/Keywords/ for more
$mainMod = Mod1

# Example binds, see https://wiki.hyprland.org/Configuring/Binds/ for more


#Themes
gsettings set org.gnome.desktop.interface icon-theme 'Dracula'
gsettings set org.gnome.desktop.interface cursor-theme 'Dracula-cursors'


#Bind sound, brightness

#wob
# $WOBSOCK = /run/user/1000/wob.sock
# exec=rm -f $WOBSOCK && mkfifo $WOBSOCK && tail -f $WOBSOCK | wob 
# bind =,XF86MonBrightnessUp, exec, brightnessctl set +5% | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK
# bind =,XF86MonBrightnessDown, exec, brightnessctl set 5%- | sed -En 's/.*\(([0-9]+)%\).*/\1/p' > $WOBSOCK
# bind =,XF86AudioLowerVolume,  exec, pactl set-sink-volume @DEFAULT_SINK@ -5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1 | awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK 
# bind =,XF86AudioRaiseVolume,  exec, pactl set-sink-volume @DEFAULT_SINK@ +5% && pactl get-sink-volume @DEFAULT_SINK@ | head -n 1| awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK 
# bind =,XF86AudioMute,         exec, pactl set-sink-mute @DEFAULT_SINK@ toggle && pactl get-sink-mute @DEFAULT_SINK@ | awk '{print substr($5, 1, length($5)-1)}' > $WOBSOCK
# bind =,XF86AudioMicMute,      exec, pactl set-source-mute @DEFAULT_SOURCE@ toggle 

# avizo
bind =,XF86MonBrightnessUp, exec, /usr/bin/lightctl up 5%
bind =,XF86MonBrightnessDown, exec, /usr/bin/lightctl down 5%
binde=,XF86AudioLowerVolume,  exec, /usr/bin/volumectl -u down 5%
binde=,XF86AudioRaiseVolume,  exec, /usr/bin/volumectl -u up 5%
bind =,XF86AudioMute,         exec, /usr/bin/volumectl toggle-mute
bind =,XF86AudioMicMute,      exec, /usr/bin/volumectl -m toggle-mute

#swayosd
# bind =, XF86AudioRaiseVolume, exec, swayosd-client --output-volume +5
# bind =, XF86AudioLowerVolume, exec,  swayosd-client --output-volume -5
# bind =, XF86AudioMute, exec,swayosd-client --output-volume mute-toggle
# bind =, XF86AudioMicMute, exec, swayosd-client --input-volume mute-toggle
# bind =, Caps_Lock, exec, swayosd-client --caps-lock
# bind =, XF86MonBrightnessUp,  exec, swayosd-client --brightness 5
# bind =, XF86MonBrightnessDown, exec, swayosd-client --brightness -5

#Bind Programms
bind = $mainMod,    w,          exec, firefox
bind = $mainMod,    t,          exec, konsole
bind = $mainMod,    x,          exec, wlogout
bind = $mainMod,    d,          exec, wofi --show drun -p Search -W 1000 -H 500 -I -m -t kitty -G -l 0 -O alphabetical
bind = $mainMod,    c,          exec, gnome-calculator
bind = $mainMod,    b,          exec, kitty -e bluetuith

bind = $mainMod     SHIFT,  w,      exec, chromium
bind = $mainMod     SHIFT,  r,      exec, hyprctl reload
bind = $mainMod     SHIFT,  t,      exec, tor-browser
bind = $mainMod     SHIFT,  k,      exec, kitty -e calcure
bind = $mainMod     SHIFT,  s,      exec, kitty -e ~/.config/scripts/hardware/cpu_freq.sh
bind = $mainMod     SHIFT,  b,      exec, kitty -e ~/.config/scripts/other_scripts/blut.sh
bind = $mainMod     SHIFT,  z,      exec, kitty -e ~/.config/scripts/vpn/vpn_s7.sh
bind = $mainMod     SHIFT,  Return, exec, thunar

bind = $mainMod     CONTROL, b,         exec, keepassxc
bind = $mainMod     CONTROL, d,         exec, discord
bind = $mainMod     CONTROL, z,         exec, kitty -e htop
bind = $mainMod     CONTROL, x,         exec, kitty -e bpytop
bind = $mainMod     CONTROL, c,         exec, kitty -e btop

bind = $mainMod,    Return,     exec, kitty

#Bind Kill
bind = $mainMod, q, killactive, 
bind = $mainMod SHIFT CONTROL, q, exit, 
bind = $mainMod, n, exec, killall -SIGUSR1 waybar

#Bind Screenshots
bind = $mainMod     SHIFT,  Print,  exec, grim ~/Pictures/Screenshots/screen-"$(date +%s)".png | swappy -f -
bind = $mainMod,    Print,          exec, grim -g "$(slurp)" - | swappy -f - "~/Pictures/Screenshots/screen-'$(date +%s)'.png"

#Bind Floating
bind = $mainMod, p, pseudo, # dwindle
bind = $mainMod, o, togglesplit, # dwindle
bind = $mainMod, v, togglefloating, 

# Move focus with mainMod + arrow keys
bind = $mainMod, h, movefocus, l
bind = $mainMod, l, movefocus, r
bind = $mainMod, k, movefocus, u
bind = $mainMod, j, movefocus, d

#move windows
bind = $mainMod SHIFT, h, movewindow, l
bind = $mainMod SHIFT, l, movewindow, r
bind = $mainMod SHIFT, k, movewindow, u
bind = $mainMod SHIFT, j, movewindow, d

#resize windows
bind = $mainMod CTRL, h, resizeactive, -20 0
bind = $mainMod CTRL, l, resizeactive, 20 0
bind = $mainMod CTRL, k, resizeactive, 0 -20
bind = $mainMod CTRL, j, resizeactive, 0 20

# Switch workspaces with mainMod + [0-9]
bind = $mainMod, 1, workspace, 1
bind = $mainMod, 2, workspace, 2
bind = $mainMod, 3, workspace, 3
bind = $mainMod, 4, workspace, 4
bind = $mainMod, 5, workspace, 5
bind = $mainMod, 6, workspace, 6
bind = $mainMod, 7, workspace, 7
bind = $mainMod, 8, workspace, 8
bind = $mainMod, 9, workspace, 9
bind = $mainMod, 0, workspace, 10

# Move active window to a workspace with mainMod + SHIFT + [0-9]
bind = $mainMod SHIFT, 1, movetoworkspace, 1
bind = $mainMod SHIFT, 2, movetoworkspace, 2
bind = $mainMod SHIFT, 3, movetoworkspace, 3
bind = $mainMod SHIFT, 4, movetoworkspace, 4
bind = $mainMod SHIFT, 5, movetoworkspace, 5
bind = $mainMod SHIFT, 6, movetoworkspace, 6
bind = $mainMod SHIFT, 7, movetoworkspace, 7
bind = $mainMod SHIFT, 8, movetoworkspace, 8
bind = $mainMod SHIFT, 9, movetoworkspace, 9
bind = $mainMod SHIFT, 0, movetoworkspace, 10

# Scroll through existing workspaces with mainMod + scroll
bind = $mainMod, mouse_down, workspace, e-1
bind = $mainMod, mouse_up, workspace, e+1

# Move/resize windows with mainMod + LMB/RMB and dragging
bindm = $mainMod, mouse:272, movewindow
bindm = $mainMod, mouse:273, resizewindow
